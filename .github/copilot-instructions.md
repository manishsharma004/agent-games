# Repository Instructions For Coding Agents

- Use Bun for all package and script commands in this repository.
- Always run installs with `bun install`.
- Always run scripts with `bun run <script>`.
- Do not use `npm`, `yarn`, or `pnpm` commands.